name: veille-droit-affaires

on:
  schedule:
    - cron: "0 7 * * 1-5"
    - cron: "0 8 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  gate:
    runs-on: ubuntu-latest
    outputs:
      go: ${{ steps.check.outputs.go }}
    steps:
      - id: check
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "go=true" >> "$GITHUB_OUTPUT"
          elif [ "$(TZ=Europe/Paris date +%H)" = "09" ]; then
            echo "go=true" >> "$GITHUB_OUTPUT"
          else
            echo "go=false" >> "$GITHUB_OUTPUT"
          fi

  digest:
    needs: gate
    if: needs.gate.outputs.go == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install requests python-dotenv

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
                            @              laude-code

      - name: Recuperer      - name: Recupere          PISTE_CLIENT_ID: ${{ secrets.PISTE_CLIE      - na         PISTE_C      - name: Recupecrets.PISTE_CLIENT_SECRET }}
        run: python scripts/fetch_jorf.py

      - name: Re      - name: Re   dence
        env:
          PISTE_CLIENT_ID: ${{   crets.PISTE_C          PISTE_CLIENT_ID: ${{   crets.PISTE_C          PISTE_CLIENT_ICR          PISTE_CLIENT_ID: ${{   crets.PISTi.py          PISTE_CLIENT_ID: ${{   crets.PISTE_C           ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          PISTE_CLIENT_ID: ${{ secrets.PISTE_CLIENT_ID }}
          PISTE_CLI        ET         rets.PISTE_CLIENT_SECRET }}
        run: claude        run: claude    .md)" --allowed        run: claude        run: sion-mode acceptEdits --max-turns 20

               nv               nv                               nv               nv            Y }}
          DIGEST_FROM: ${{ secrets.DIGEST_FROM }}
          DIGEST_TO: ${{ se          DIGEST_TO: ${{ se          DIGEST_TO: ${{ dig          DIGEST_TO: ${{ se          DIGEST_TO: ${{ se          DIGEST_TO: ${{ dig          DIGEST_TO: ${{ se          DIGEST_TO: ${{ se          DIGEST_TO: ${{ dig  .no          DIGEST_TO: ${{ se          DIGe/j          DIGEST_TO: ${{ se        -m "maj registre ju          DIGEST_TO: ${{ se          DIGEST_TO: ${{ se          DIGEST_TO: ${{ dig          DIGEST_TO: ${{ se          DIGEST_TO: ${{ se          DIGEST_TO: ${{ dig          DIGEST_TO: ${{ se          DIGEST_TO: ${{ se          DIGEST_TO: ${{ dig  .no          DIGEST_TO}
          DIGEST_TO: ${{ secrets.DIGEST_TO }}
        run: curl -s -X POST https://api.resend.com/emails -H "Authorization: Be        run: curl -s -X POST https://api.resend.com/emails -H "Authorization: Be        run: curl -s -X POST https://api.res\"[        run: curl t des affaires\",\"html\":\"<p>Le pipeline a echoue. Logs GitHub Actions.</p>\"}"
